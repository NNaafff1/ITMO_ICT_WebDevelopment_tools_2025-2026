# Practice 1 — FastAPI Basics
Demonstrates basic FastAPI: routing, Pydantic models, path/query params, CRUD with in-memory storage.

## Run
```bash
cd practice_1
pip install fastapi uvicorn
uvicorn src.main:app --reload --port 8001
```
