# Practice 2 — Database Integration
Demonstrates SQLAlchemy ORM with SQLite, Pydantic schemas, dependency injection for DB session, pagination.

## Run
```bash
cd practice_2
pip install -r requirements.txt
uvicorn src.main:app --reload --port 8002
```
